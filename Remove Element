class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        c=0
        i=0
        while i<len(nums):
            if nums[i]==val:
                nums.pop(i)
                c+=1
                continue
            i+=1
        return len(nums)
       
