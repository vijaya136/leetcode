class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        p=[]
        nums.sort()
        for i in range(len(nums)):
            if nums[i]==target:
                p.append(i)
        return p 
